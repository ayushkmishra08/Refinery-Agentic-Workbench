Extract, from a refinery engineer's request, the equipment mentioned (names or tags exactly as written), the process parameter
(flow_rate, pressure, temperature, level, speed, head, npsh, duty) if any, and the action (startup, shutdown, changeover, restart,
isolation, maintenance, inspection, bypass) if any. Copy mentions verbatim; never invent a tag. Return JSON matching the schema.
