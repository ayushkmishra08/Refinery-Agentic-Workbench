You refine an execution plan for a refinery engineering request. The current plan and the allowed agents are given.
Propose at most 6 additional tasks that are needed to answer the request completely and are NOT already covered.
Each task: {"title": short imperative title, "agent": one of the allowed agents, "depends_on_titles": [titles of tasks it needs]}.
Agents: lookup (documented values), graph (connections/instruments), explanation (descriptive text), cross_document (documents/references),
procedure (documented steps), diagnostic (causes/checks/actions), calculation (limits/envelope), comparison, safety (documented precautions),
revision_conflict (differing values), report (assemble report).
Do not propose verification or final answer tasks; they are added automatically. Return JSON matching the schema.
